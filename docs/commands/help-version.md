# help and version

Show help or version information.

## Usage

```sh
repovista help
repovista version
repovista --help
repovista --version
```

## Examples

```sh
repovista help
repovista version
```

## Notes

`help` renders the same command and option registry used by the CLI parser, so it is the fastest local source of truth for installed behavior.
